# 前后端服务健康检查与登录验证 - 验证清单

## 基础服务
- [x] MySQL 服务正在运行
- [x] MySQL `SELECT 1` 查询成功
- [x] Redis 服务正在运行
- [x] Redis `PING` 返回 `PONG`

## 后端服务
- [x] 后端服务在 8080 端口运行
- [x] `/captchaImage` 接口返回 HTTP 200
- [x] 响应包含验证码数据或 captchaEnabled 字段

## 前端服务
- [x] 前端服务在 8081 端口运行
- [x] 根路径 `/` 返回 HTTP 200
- [x] 返回 HTML 包含「若依管理系统」标题
- [x] JS/CSS 静态资源正确加载（返回 200）
- [x] 不存在的静态文件正确返回 404（而非返回 HTML）

## API 代理
- [x] `/dev-api/captchaImage` 通过 8081 端口返回 HTTP 200
- [x] 代理转发的响应与直接访问后端一致

## 浏览器渲染
- [x] 浏览器打开登录页面正常显示
- [x] 页面包含账号输入框
- [x] 页面包含密码输入框
- [x] 页面包含验证码（启用时）
- [x] 页面包含登录按钮
- [x] 浏览器控制台无 JavaScript 错误

## 登录功能
- [x] 验证码可通过系统配置关闭
- [x] 使用 admin/admin123 可成功登录
- [x] 登录成功后跳转到 `/index` 首页
- [x] 首页显示侧边栏菜单（首页、系统管理、业务管理等）
- [x] 验证完成后验证码配置已恢复为开启状态
